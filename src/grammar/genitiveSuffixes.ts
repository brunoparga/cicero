// To keep data separately from logic, the suffixes that identify which group a noun belongs to
// are kept here.
const singular = ['-ae', '-ī', '-is', '-ūs', '-ēī'];
const plural = ['-ārum', '-ōrum', '-(i)um', '-uum', '-ērum'];

export { singular, plural };
