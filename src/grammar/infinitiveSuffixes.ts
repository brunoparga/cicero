// To keep data separately from logic, the suffixes that identify which group a verb belongs to
// are kept here.
const active = ['-āre', '-ēre', '-ere', '-īre'];
const passive = ['-āri', '-ērī', '-ī', '-īrī'];

export { active, passive };
